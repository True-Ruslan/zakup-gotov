# v0.1.0-rc.4 release failure — 2026-08-18

## Verdict

**FAILED CLOSED AT RELEASE METADATA VALIDATION.**

`v0.1.0-rc.4` is historical immutable release evidence. Its tag must not be deleted, moved or reused.

## Exact source

Tag:

```text
v0.1.0-rc.4
```

Exact commit:

```text
8a269288addcb4aa8ea3d0ce46608b650cbdb6dc
```

GitHub comparison shows the tag and the selected rc.4 source are identical (`ahead=0`, `behind=0`).

## Release event

GitHub Actions run:

```text
Release / run 32136955056
```

Event metadata:

- event: `release`;
- head tag: `v0.1.0-rc.4`;
- head SHA: `8a269288addcb4aa8ea3d0ce46608b650cbdb6dc`;
- conclusion: `failure`.

`Release / Verify` failed at its first contract step, `Validate release metadata`. All later verification steps were skipped. `Release / Publish` was skipped entirely.

The failing invocation contained:

```text
--tag "v0.1.0-rc.4"
--prerelease "false"
```

and the release contract rejected it with:

```text
ValueError: GitHub prerelease flag must match the SemVer prerelease state
```

## Root cause

The Git tag is a SemVer prerelease, but the GitHub Release was published with `prerelease=false`.

This is an operator/release-metadata defect, not a source, tag-target, repository-verification, container or security failure.

The release contract behaved correctly by stopping before any write-capable publication work.

## Proven negative effects

Because `Release / Publish` never started, rc.4 did **not**:

- publish or promote new final GHCR images;
- mutate the OCI `latest` tag;
- produce release SBOMs or attestations;
- perform staging/final exact-digest smoke tests;
- attach release evidence/checksum assets;
- establish package visibility evidence.

## GitHub release metadata side effect

GitHub release id `372327860` was published with:

```text
draft=false
prerelease=false
```

Therefore GitHub temporarily treats rc.4 as the repository's `Latest release`. This is GitHub Release presentation metadata only and must not be confused with the OCI `latest` tag.

The presentation should be corrected by editing the existing rc.4 release, enabling **Set as a pre-release**, and saving. The tag itself must remain untouched. Correcting the presentation does not convert the failed workflow into an accepted release-contract run.

## Next release candidate

The next full immutable release-contract attempt is:

```text
v0.1.0-rc.5
```

A new exact source SHA must be selected from verified `main` after canonical documentation is synchronized. rc.5 must be published with the GitHub pre-release flag explicitly enabled before pressing Publish.

## Acceptance rule

`v0.1.0-rc.4` remains **FAILED** even if its GitHub presentation metadata is corrected later. Only a new release event that passes the complete existing release contract may advance the accepted release proof boundary.
